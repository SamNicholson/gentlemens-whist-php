{% extends 'base.twig' %}

{% block body %}

    <div class="container">
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">Whist</a>
                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">New <span class="fa"></span></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ base_url() }}/players/add">Player</a></li>
                                <li><a href="{{ base_url() }}/games/create">Game</a></li>
                            </ul>
                        </li>
                        <li><a href="{{ base_url() }}/games">Games</a></li>
                        <li><a href="{{ base_url() }}/players/update">Profile</a></li>
                        <li><a href="{{ base_url() }}">Logout</a></li>
                    </ul>
                </div><!--/.nav-collapse -->
            </div><!--/.container-fluid -->
        </nav>
        <div class="row">
            <div class="col-sm-12 text-center">
                {% if not hideLogo %}
                    <img src="{{ base_url() }}/img/logo.png">
                {% endif %}
            </div>
        </div>
        {% block content %}{% endblock %}
    </div>
{% endblock %}